/// Enum for the type of identifier
/// Enum values like email, phone are used when parsing the identifier to json
/// This is used in the [BasicLoginParams] class
library;

enum IdentifierType { email, phone }
